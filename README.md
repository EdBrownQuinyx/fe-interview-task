# The task
- Using a react component, fetch the weatherData.json data (available at '/weatherData.json')
- Render the data as a list
- Style it using css modules

If you complete this, extra bonus points for:
- Adding filtering / sorting
- Add a danger indicator in the UI if the wind is above 80


# Example output

![An image of a list of forecasts](weatherList.png 'An image of a list of forecasts')